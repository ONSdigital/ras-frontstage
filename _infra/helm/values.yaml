replicaCount: 1

namespace: minikube

service:
  port: 8087
  type: ClusterIp

ingress:
  enabled: false
  host: surveys.example.com
  certName: frontstage-cert

networkPolicy:
  egress: |
    - {}
  ingress: |
    - {}

resources:
  requests:
    memory: "256Mi"